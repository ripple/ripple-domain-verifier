{
  "name": "ripple-domain-verifier",
  "version": "0.2.1",
  "description": "two-way cryptographic link between ripple validator and domain",
  "main": "lib/",
  "scripts": {
    "test": "mocha test/ --compilers=js:babel/register --timeout=10000",
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ripple/ripple-domain-verifier.git"
  },
  "keywords": [
    "ripple",
    "crypto",
    "money"
  ],
  "author": "Steven Zeiler",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ripple/ripple-domain-verifier/issues"
  },
  "homepage": "https://github.com/ripple/ripple-domain-verifier#readme",
  "devDependencies": {
    "babel": "^5.5.6",
    "babel-runtime": "^5.5.6",
    "mocha": "^2.2.5"
  },
  "dependencies": {
    "bluebird": "^3.4.0",
    "request-promise": "^2.0.1",
    "ripple-address-codec": "^1.6.0",
    "ripple-keypairs": "^0.7.3",
    "validator": "^3.40.0"
  }
}
